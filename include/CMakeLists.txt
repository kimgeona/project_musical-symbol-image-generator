# 디렉토리에 있는 *.hpp 파일을 INCLUDE_LIST 변수에 추가한다
file(
    GLOB_RECURSE INCLUDE_LIST CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_SOURCE_DIR}/*.hpp
)
# 변수를 상위 스코프로 올린다
set(INCLUDE_LIST ${INCLUDE_LIST} PARENT_SCOPE)